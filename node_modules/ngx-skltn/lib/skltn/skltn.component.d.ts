import { OnInit, QueryList, ElementRef, ChangeDetectorRef, AfterViewInit, NgZone, OnDestroy } from '@angular/core';
import { SkltnBoneDirective } from '../directives/skltn-bone.directive';
import { DomSanitizer, SafeHtml } from '@angular/platform-browser';
import { Subject } from 'rxjs';
import { SkltnConfig } from '../interfaces/skltn-config';
import { SkltnService } from '../services/skltn.service';
export declare class SkltnComponent implements OnInit, OnDestroy, AfterViewInit {
    private skltnService;
    private element;
    private sanitizer;
    private cd;
    private zone;
    rectRadius: number;
    bgFill: string;
    flareFill: string;
    flareWidth: string;
    duration: number;
    delay: number;
    timing: string;
    showSkltn: boolean;
    bones: QueryList<SkltnBoneDirective>;
    viewBox: string;
    shapes: any[];
    parentClientRect: any;
    animationCss: SafeHtml;
    href: string;
    sufix: string;
    gradientId: string;
    maskId: string;
    checkHrefIntervalID: number;
    updStream$: Subject<{}>;
    defaultConfig: SkltnConfig;
    constructor(skltnService: SkltnService, element: ElementRef, sanitizer: DomSanitizer, cd: ChangeDetectorRef, zone: NgZone);
    onResize(event: Event): void;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    getSufixWithID(): string;
    calcShapes(): void;
}
